O9010 goes in the subroutines folder.

The three G code files access that sub all three ways.

Sub imbedded in the file (subtest.tap)
Sub accessed with O number (subtest-external.tap) and
Sub accessed with a file name (subtest-file.tap).